{
  "tools": [
    {
      "type": "function",
      "name": "outlook_list_skills",
      "description": "Find user-managed local skills about employees, responsibilities, processes and communication. First search by person, email, topic, project or client, then call outlook_load_skill with relevant IDs. Returns only names and descriptions for selection, not full knowledge. Omit query to discover enabled, currently active skills.",
      "parameters": {
        "type": "object", "properties": { "query": { "type": "string", "description": "Relevant person, email, process, project or subject." } }, "additionalProperties": false
      }
    },
    {
      "type": "function",
      "name": "outlook_load_skill",
      "description": "Load full instructions and structured people from 1–5 enabled local skills selected from outlook_list_skills. Respect project, client and validity dates. Explicit assignments in emails override skill defaults. Cite skill names and email evidence; label uncertainty and conflicts. A skill does not authorize Outlook writes.",
      "parameters": {
        "type": "object", "properties": { "ids": { "type": "array", "items": { "type": "string" }, "minItems": 1, "maxItems": 5 } }, "required": ["ids"], "additionalProperties": false
      }
    },
    {
      "type": "function",
      "name": "outlook_propose_skill_update",
      "description": "Propose an editable draft update to an already loaded skill when current emails provide evidence of changed responsibilities or rules. Does NOT save or change any knowledge. Explain the evidence in reason. Supply complete revised instructions, preserving unaffected content. The user must review and explicitly save the draft.",
      "parameters": {
        "type": "object",
        "properties": {
          "id": { "type": "string" },
          "reason": { "type": "string", "description": "Why the skill may be stale, citing sender, date and short email evidence." },
          "instructions": { "type": "string", "description": "Complete revised skill instructions." },
          "people": { "type": "array", "description": "Optional complete revised people list. Omit to preserve existing people. Never invent email addresses.", "items": {
            "type": "object", "properties": {
              "email": { "type": "string" }, "name": { "type": "string" }, "role": { "type": "string" },
              "aliases": { "type": "array", "items": { "type": "string" } },
              "responsibilities": { "type": "array", "items": { "type": "string" } }
            }, "required": ["email", "name"], "additionalProperties": false
          } }
        }, "required": ["id", "reason", "instructions"], "additionalProperties": false
      }
    },
    {
      "type": "function",
      "name": "outlook_read_conversation",
      "description": "Read the Outlook conversation of a selected or open email or meeting invitation by Conversation ID across accessible folders/stores including sent and archive, up to 100 recent items returned chronologically. Use for discussion analysis, responsibilities, commitments, deadlines or checking completion; a single email is insufficient to establish missing completion. No subject-only search. Report coverage_note, truncation and unavailable messages. Attachment contents are not analyzed.",
      "parameters": {
        "type": "object", "properties": {
          "message_id": { "type": "string", "description": "Known message ID, or omit for the currently selected/open item." },
          "max_items": { "type": "integer", "minimum": 1, "maximum": 100, "description": "Default 100." }
        }, "additionalProperties": false
      }
    },
    {
      "type": "function",
      "name": "outlook_get_current_compose_state",
      "description": "Read the current compose-window state (subject, recipients, body, thread, attachments, current_user, item_type, direction, my_role). Set include_full_body=true for the full body instead of a 1000-char summary.",
      "parameters": {
        "type": "object",
        "properties": {
          "include_full_body": {
            "type": "boolean"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "type": "function",
      "name": "outlook_list_folders",
      "description": "List the user's mail folders (max depth 6, max 200 nodes).",
      "parameters": {
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "type": "function",
      "name": "outlook_search_messages",
      "description": "Search messages. Combine any subset of filters via AND. Returns id+metadata+snippet for up to max_results (default 25, hard cap 100). ALWAYS translate the user's natural-language query into the structured fields below; do NOT dump the whole user sentence into 'query'. PRESERVE the user's exact spelling and whitespace for company / vendor / brand names - 'IT Creations' is one phrase with a space; do NOT compact it to 'ITCreations'. Whitespace is a literal character in the Outlook LIKE match. Examples: User says 'what was my first email ever' -> {scope:'all_mail', sort_order:'oldest', max_results:1}. User says 'emails from Alice last week' -> {from:'Alice', date_from:<7d ago ISO>, date_to:<today ISO>}. User says 'messages I sent to Susan about servers' -> first use outlook_list_folders to find Sent Items/Outbound, then {to:'Susan', query:'servers', folder_id:<sent folder id>, max_results:25}; do not put recipient in query or from. For multiple recipients, make separate precise searches. User says 'quotes from IT Creations' (vendor lookup) -> {from:'IT Creations', scope:'all_mail'}. Put the vendor name in 'from', not 'query'; 'from' matches both display name and email address case-insensitively. User says 'email from before 2020 with the EIN' -> {query:'EIN', date_to:'2020-01-01T00:00:00Z'}. User says 'find an email with EIN' -> {query:'EIN', scope:'auto'}; if zero, try {query:'Employer Identification Number', scope:'auto'}. User says 'unread invoices' -> {body_contains:'invoice', read_status:'unread'}. User says 'flagged messages with attachments' -> {flag_status:'flagged', attachment_filter:'with'}. Zero-result fallback ladder: if a search returns 0 hits, do NOT report 'no emails' immediately - try at least three variations: (1) drop any AND filters you added beyond what the user explicitly asked for (e.g. body_contains:'quote' if they said 'quotes from X', try without it first); (2) move the company name from 'query' to 'from' (or vice versa) - vendors usually match 'from'; (3) try a compact spelling without spaces only AFTER the user's exact spelling fails (e.g. 'IT Creations' first, then 'itcreations'); (4) broaden scope to all_mail if you started with auto. Never send default false filters such as has_attachment:false or is_unread:false; use attachment_filter/read_status/flag_status/importance_filter and use 'any' when not requested. For 'I sent' / sent / outgoing report requests, prefer outlook_list_folders to discover Sent Items or Outbound and pass folder_id rather than scope:'all_mail' when possible. The result includes total_matches and truncated. If truncated is true you did NOT get all matches - the array is capped at max_results (hard cap 100). When the user wants a COMPLETE list or spreadsheet, do NOT try to raise max_results past 100; instead call outlook_export_search_results, which collects the full set server-side. If you pass NO filters, the tool returns the newest 25 messages in the current folder - almost never what the user asked for. Prefer one precise call over many. After search, use outlook_read_message on the most-relevant id for full body. For tabular Excel exports over many messages, prefer metadata-only synthesis: the snippet field already contains the first ~200 chars of each match, so build Excel rows directly from search results and only call outlook_read_messages when a column genuinely requires full body content. Reading full bodies for 100+ messages will exceed the context window. When body content really is required for an Excel/PDF report over many matches, page by date window: do separate searches per quarter / month, read up to 25 bodies per window, extract rows, accumulate across turns, then call the export tool once at the end.",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Free-form keyword(s) matched against subject + body (e.g. 'EIN', 'invoice', 'contract'). Do NOT put dates, sender names, recipient names, or other structured info here - use the dedicated fields."
          },
          "from": {
            "type": "string",
            "description": "Sender substring; matches display name OR email (case-insensitive). Example: 'Alice' or 'alice@example.com'."
          },
          "to": {
            "type": "string",
            "description": "Recipient substring; for multiple recipients, make separate precise searches rather than concatenating names."
          },
          "subject_contains": {
            "type": "string",
            "description": "Substring match on subject only."
          },
          "body_contains": {
            "type": "string",
            "description": "Substring match on body only."
          },
          "scope": {
            "type": "string",
            "enum": [
              "current_folder",
              "all_mail",
              "auto"
            ],
            "description": "Default auto. Use all_mail directly for 'ever', 'any email', 'anywhere', 'everything', or 'all mail'. folder_id overrides scope."
          },
          "sort_order": {
            "type": "string",
            "enum": [
              "newest",
              "oldest"
            ],
            "description": "Default newest. Use oldest for 'first', 'earliest', or 'oldest'."
          },
          "attachment_filter": {
            "type": "string",
            "enum": [
              "any",
              "with",
              "without"
            ],
            "description": "Use with only when the user asks for attachments; without only when they ask for no attachments; otherwise any."
          },
          "read_status": {
            "type": "string",
            "enum": [
              "any",
              "read",
              "unread"
            ],
            "description": "Use unread/read only when explicitly requested; otherwise any."
          },
          "flag_status": {
            "type": "string",
            "enum": [
              "any",
              "flagged",
              "unflagged"
            ],
            "description": "Use flagged/unflagged only when explicitly requested; otherwise any."
          },
          "importance_filter": {
            "type": "string",
            "enum": [
              "any",
              "low",
              "normal",
              "high"
            ],
            "description": "Use low/normal/high only when explicitly requested; otherwise any."
          },
          "folder_id": {
            "type": "string",
            "description": "Default: Inbox. Use outlook_list_folders to discover folder ids."
          },
          "date_from": {
            "type": "string",
            "format": "date-time",
            "description": "Inclusive lower bound, ISO-8601 UTC. For 'last week' compute 7 days ago. For 'this year' use Jan 1 of current year."
          },
          "date_to": {
            "type": "string",
            "format": "date-time",
            "description": "Exclusive upper bound, ISO-8601 UTC. For 'before 2020' use '2020-01-01T00:00:00Z'. For 'older than 2 years' use today minus 2y."
          },
          "max_results": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "description": "Default 25. Raise to 100 only when summarizing a folder; never for a targeted lookup."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "type": "function",
      "name": "outlook_read_message",
      "description": "Fetch one message by id. Body always plaintext; truncated at 32 KB. Response includes current_user, item_type, direction, my_role, mail role flags (is_from_me/is_to_me/is_cc_to_me), and meeting fields when applicable.",
      "parameters": {
        "type": "object",
        "required": [
          "message_id"
        ],
        "properties": {
          "message_id": {
            "type": "string"
          },
          "include_full_body": {
            "type": "boolean"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "type": "function",
      "name": "outlook_count_messages",
      "description": "Count messages matching the given filters without returning bodies. Same filter fields as outlook_search_messages; same rule: translate the user's intent into structured fields, do NOT put everything in 'query'. Examples: 'how many unread from Bob' -> {from:'Bob', read_status:'unread'}; 'how many emails this year' -> {date_from:<Jan 1 ISO>}. Empty filters => current-folder count unless scope:'all_mail'.",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string"
          },
          "from": {
            "type": "string"
          },
          "to": {
            "type": "string",
            "description": "Recipient substring; for multiple recipients, make separate precise searches rather than concatenating names."
          },
          "subject_contains": {
            "type": "string"
          },
          "body_contains": {
            "type": "string"
          },
          "scope": {
            "type": "string",
            "enum": [
              "current_folder",
              "all_mail",
              "auto"
            ]
          },
          "sort_order": {
            "type": "string",
            "enum": [
              "newest",
              "oldest"
            ]
          },
          "attachment_filter": {
            "type": "string",
            "enum": [
              "any",
              "with",
              "without"
            ]
          },
          "read_status": {
            "type": "string",
            "enum": [
              "any",
              "read",
              "unread"
            ]
          },
          "flag_status": {
            "type": "string",
            "enum": [
              "any",
              "flagged",
              "unflagged"
            ]
          },
          "importance_filter": {
            "type": "string",
            "enum": [
              "any",
              "low",
              "normal",
              "high"
            ]
          },
          "folder_id": {
            "type": "string"
          },
          "date_from": {
            "type": "string",
            "format": "date-time"
          },
          "date_to": {
            "type": "string",
            "format": "date-time"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "type": "function",
      "name": "outlook_read_messages",
      "description": "Bulk-read message details by short ID array. Returns subject/sender/date/body/attachments per id. Response includes current_user, item_type, direction, my_role, mail role flags (is_from_me/is_to_me/is_cc_to_me), and meeting fields when applicable. Use this instead of multiple outlook_read_message calls; 5-10x faster when you have many IDs from a prior outlook_search_messages and you need bodies (action items, topic-status, conversation summary reports). Avoid calling on more than ~25 IDs when each body is long. For large result sets you intend to export, work from search metadata + snippet instead of bulk-reading bodies.",
      "parameters": {
        "type": "object",
        "required": [
          "ids"
        ],
        "properties": {
          "ids": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Short message IDs from a prior outlook_search_messages."
          },
          "include_body": {
            "type": "boolean",
            "description": "Default true. Set false for a lightweight metadata-only read."
          },
          "max_items": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "description": "Default 25, hard cap 100."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "type": "function",
      "name": "outlook_aggregate_messages",
      "description": "Group matching messages by sender, day, or folder and return the top-N buckets by count. Use this instead of calling outlook_count_messages many times when the user wants statistics ('top 10 senders this month', 'busiest days last week', 'breakdown by folder').",
      "parameters": {
        "type": "object",
        "required": [
          "group_by"
        ],
        "properties": {
          "scope": {
            "type": "string",
            "enum": [
              "auto",
              "current_folder",
              "all_mail"
            ],
            "description": "Default auto."
          },
          "folder_id": {
            "type": "string",
            "description": "Optional explicit folder id. Overrides scope when provided."
          },
          "date_from": {
            "type": "string",
            "format": "date-time",
            "description": "Optional lower bound on ReceivedTime."
          },
          "date_to": {
            "type": "string",
            "format": "date-time",
            "description": "Optional upper bound on ReceivedTime."
          },
          "from": {
            "type": "string",
            "description": "Optional sender name or email substring."
          },
          "subject_contains": {
            "type": "string",
            "description": "Optional subject substring."
          },
          "body_contains": {
            "type": "string",
            "description": "Optional body substring."
          },
          "group_by": {
            "type": "string",
            "enum": [
              "sender",
              "day",
              "folder"
            ],
            "description": "How to bucket matching messages."
          },
          "top_n": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "description": "Default 10, hard cap 100."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "type": "function",
      "name": "outlook_export_excel",
      "description": "Create a spreadsheet Excel .xlsx tabular export. Use when the user asks for spreadsheet, Excel, xlsx, or tabular export; pass typed columns and rows. Produces a styled workbook with bold/frozen header, autofilter, and per-column formatting. Best for vendor lists, message tables, aggregations by sender/day, and structured search-result exports. Do NOT use for prose, narrative reports, or arbitrary text; choose outlook_export_pdf for those. Maximum 10000 rows; aggregate or filter first if more. Example flow: search messages first, then export projected rows. File is saved to ~\\Documents\\OutlookAI\\Reports\\; the UI surfaces Open/Show-in-folder later. For exports over 50+ messages, prefer projecting columns (subject, from, to, received_at, snippet) from outlook_search_messages results without reading full bodies; this is metadata-only synthesis and stays within the context window. Reading full bodies for 100+ messages will exceed context. For exports that genuinely require body-derived columns over a large result set, page by date window across multiple turns (search Q1 -> read 25 -> extract rows; search Q2 -> read 25 -> extract rows; ... -> outlook_export_excel once with all accumulated rows).",
      "parameters": {
        "type": "object",
        "required": [
          "columns",
          "rows"
        ],
        "properties": {
          "filename_hint": {
            "type": "string",
            "description": "Optional base filename; sanitized and timestamped, with a safe default when omitted."
          },
          "sheet_name": {
            "type": "string",
            "description": "Optional worksheet name; defaults to filename_hint."
          },
          "columns": {
            "type": "array",
            "description": "Column definitions in display order. Type controls Excel formatting: date/datetime for ISO dates, number/currency for numeric cells, boolean for true/false, text otherwise.",
            "items": {
              "type": "object",
              "required": [
                "name",
                "type"
              ],
              "properties": {
                "name": {
                  "type": "string",
                  "description": "Column header text."
                },
                "type": {
                  "type": "string",
                  "enum": [
                    "text",
                    "date",
                    "datetime",
                    "number",
                    "currency",
                    "boolean"
                  ],
                  "description": "Cell type used for validation and per-column Excel formatting."
                }
              },
              "additionalProperties": false
            }
          },
          "rows": {
            "type": "array",
            "description": "Row arrays; each row length must equal columns.length. Cells may be strings, numbers, ISO dates, or booleans matching the column type.",
            "items": {
              "type": "array"
            }
          }
        },
        "additionalProperties": false
      }
    },
    {
      "type": "function",
      "name": "outlook_export_pdf",
      "description": "Save a polished, printable PDF report. Use when the user asks for PDF, printable, document, or a shareable report. Pass polished markdown with a clear title, headings, tables, and lists; the tool renders a clean A4 document with a header bar and no chat UI chrome. Prefer outlook_export_excel for tabular structured data, real spreadsheets, Excel workbooks, and rows/columns users may sort or analyze; use PDF for narrative reports, summaries, action items, digest-style outputs, and client-ready documents. You may compose fresh markdown specifically for the PDF rather than reusing chat text. Example flow: search/read messages for a weekly or customer report, synthesize sections and action items as markdown, then export the PDF. File is saved to ~\\Documents\\OutlookAI\\Reports\\; the UI surfaces an Open/Show-in-folder card. Max content_markdown length: 250000 chars.",
      "parameters": {
        "type": "object",
        "required": [
          "content_markdown"
        ],
        "properties": {
          "filename_hint": {
            "type": "string",
            "description": "Optional base filename; sanitized and timestamped. Defaults to title or OutlookAI-Report when omitted."
          },
          "title": {
            "type": "string",
            "maxLength": 200,
            "description": "Optional but recommended report title, max 200 chars."
          },
          "subtitle": {
            "type": "string",
            "maxLength": 400,
            "description": "Optional report subtitle, max 400 chars."
          },
          "content_markdown": {
            "type": "string",
            "maxLength": 250000,
            "description": "Required markdown body, max 250000 chars. Supports headings, GFM tables, lists, blockquotes, code blocks, bold/italic, and links; inline images are stripped."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "type": "function",
      "name": "outlook_export_search_results",
      "description": "Export a COMPLETE list of messages matching a search filter to a styled Excel .xlsx, up to a server-configured ceiling (default 2000 rows). Use this whenever the user wants 'all', 'every', 'a list of', or 'a spreadsheet of' messages matching criteria and completeness matters - e.g. 'Excel of every email from IT Creations', 'list all messages I sent to Susan in 2025'. Unlike calling outlook_search_messages then outlook_export_excel (which caps at 100 results and silently drops the rest), this tool counts the true total and collects up to the ceiling server-side in one call, then reports how many it exported vs the true total. Pass the SAME filter fields as outlook_search_messages (from, to, query, subject_contains, body_contains, scope, date_from, date_to, folder_id) plus a 'columns' array choosing from: subject, from, to, received_at, snippet, has_attachments, folder. Columns default to received_at/from/to/subject/snippet when omitted. This tool projects only those mechanical fields (it does NOT read full bodies and cannot summarize or synthesize). For a report that needs per-message AI summaries, instead page outlook_search_messages by date window, read up to 25 bodies per window, accumulate, and call outlook_export_pdf once. If the result says truncated:true, tell the user you exported N of M and suggest narrowing the date range to capture the rest.",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Free-form keyword(s) matched against subject + body. Use dedicated fields for sender/recipient/dates."
          },
          "from": {
            "type": "string",
            "description": "Sender substring; matches display name OR email (case-insensitive)."
          },
          "to": {
            "type": "string",
            "description": "Recipient substring."
          },
          "subject_contains": {
            "type": "string"
          },
          "body_contains": {
            "type": "string"
          },
          "scope": {
            "type": "string",
            "enum": [
              "current_folder",
              "all_mail",
              "auto"
            ],
            "description": "Default auto. Use all_mail for 'ever'/'any'/'everything'. folder_id overrides scope."
          },
          "folder_id": {
            "type": "string",
            "description": "Use outlook_list_folders to discover ids."
          },
          "date_from": {
            "type": "string",
            "format": "date-time",
            "description": "Inclusive lower bound, ISO-8601 UTC."
          },
          "date_to": {
            "type": "string",
            "format": "date-time",
            "description": "Exclusive upper bound, ISO-8601 UTC."
          },
          "columns": {
            "type": "array",
            "items": {
              "type": "string",
              "enum": [
                "subject",
                "from",
                "to",
                "received_at",
                "snippet",
                "has_attachments",
                "folder"
              ]
            },
            "description": "Mechanical columns to include. Defaults to received_at, from, to, subject, snippet. 'folder' is the folder the message lives in."
          },
          "filename_hint": {
            "type": "string",
            "description": "Optional base filename, e.g. 'IT-Creations-quotes'."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "type": "function",
      "name": "outlook_get_current_selection",
      "description": "Read the items currently selected in the user's active Explorer (mail messages, meeting requests/updates/cancellations, or calendar appointments). Useful for reply/summarize/meeting context. Returns current_user, item_type, direction, my_role and meeting fields when applicable. Returns empty when nothing is selected or when there is no active Explorer.",
      "parameters": {
        "type": "object",
        "properties": {
          "include_full_bodies": {
            "type": "boolean",
            "description": "If true, returns full message body per item (up to 32 KB). Default false: 200-char snippet only."
          },
          "max_items": {
            "type": "integer",
            "minimum": 1,
            "maximum": 20,
            "description": "Hard cap on items returned. Default 5."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "type": "function",
      "name": "outlook_list_recent_threads_with",
      "description": "List the most recent conversation threads involving a specific recipient (Inbox + Sent), grouped by ConversationID.",
      "parameters": {
        "type": "object",
        "required": [
          "recipient_email"
        ],
        "properties": {
          "recipient_email": {
            "type": "string"
          },
          "max_threads": {
            "type": "integer",
            "minimum": 1,
            "maximum": 20
          }
        },
        "additionalProperties": false
      }
    },
    {
      "type": "function",
      "name": "outlook_create_draft",
      "description": "Create a draft in the Drafts folder. Never sends. If in_reply_to_message_id is given, seeds the draft via MailItem.Reply().",
      "parameters": {
        "type": "object",
        "required": [
          "subject",
          "body_plaintext"
        ],
        "properties": {
          "subject": {
            "type": "string"
          },
          "body_plaintext": {
            "type": "string"
          },
          "to": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "cc": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "in_reply_to_message_id": {
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "type": "function",
      "name": "outlook_mark_as_read",
      "description": "Set or clear the UnRead flag on a message.",
      "parameters": {
        "type": "object",
        "required": [
          "message_id",
          "read"
        ],
        "properties": {
          "message_id": {
            "type": "string"
          },
          "read": {
            "type": "boolean"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "type": "function",
      "name": "outlook_flag_message",
      "description": "Set follow-up flag status: none | todo | complete.",
      "parameters": {
        "type": "object",
        "required": [
          "message_id",
          "flag"
        ],
        "properties": {
          "message_id": {
            "type": "string"
          },
          "flag": {
            "type": "string",
            "enum": [
              "none",
              "todo",
              "complete"
            ]
          }
        },
        "additionalProperties": false
      }
    },
    {
      "type": "function",
      "name": "outlook_set_category",
      "description": "Replace a message's Categories with the single given value.",
      "parameters": {
        "type": "object",
        "required": [
          "message_id",
          "category"
        ],
        "properties": {
          "message_id": {
            "type": "string"
          },
          "category": {
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    }
  ]
}
